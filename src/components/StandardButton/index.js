import React from "react";
import { Button } from "@rneui/themed";

const StandardButton = ({ title, color, size }) => {
  return <Button title={title} color={color} size={size} />;
};
export default StandardButton;
